{
  "name": "aura-local-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "pnpm --dir backend install && pnpm --dir frontend install",
    "start": "node ./scripts/start-aura.mjs",
    "start:desktop": "pnpm --dir frontend run desktop",
    "build": "pnpm --dir backend run build && pnpm --dir frontend run build"
  }
}
